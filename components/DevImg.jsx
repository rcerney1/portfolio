const DevImg = ({ imgSrc }) => {
  return (
    <div style={{ width: '300px', height: 'auto' }}>
      <img src={imgSrc} alt='developer' style={{ width: '100%' }} />
    </div>
  );
};

export default DevImg;
